name: circular-gate

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  circular-gate:
    name: circular-gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-node-ci
      - run: npm run circular
