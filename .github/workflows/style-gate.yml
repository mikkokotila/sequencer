name: style-gate

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  style-gate:
    name: style-gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-node-ci
      - run: npm run lint
