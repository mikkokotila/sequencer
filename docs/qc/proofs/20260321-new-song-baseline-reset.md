# Proof: Deterministic New Song Baseline Reset

## Task
20260321-new-song-baseline-reset

## Summary
newSong() now calls resetAllExtensions() which resets every extension to canonical defaults with _enabled=false. Mixer setState fixed to handle `levels: null` properly, restoring faders to 0.8 instead of leaking prior levels. The fixme test is now passing.

## Changes

### src/engine/extensions/store.ts
- `CANONICAL_DEFAULTS` map with factory-initial state for all 6 extensions
- `resetAllExtensions()` calls setState(defaults) + setEnabled(false) on each

### src/engine/extensions/mixer.ts
- Fixed `setState()` to handle `levels: null` — previously ignored null, now resets to defaults and calls `applyLevels()`

### src/transport/persistence.ts
- `newSong()` calls `resetAllExtensions()` after clearing buffers

### e2e/sequencer.spec.ts
- Replaced `test.fixme` with passing deterministic test verifying pattern + extension reset

## Oracle Results (machine-generated by oracle-harness-v1)

| Oracle | Status | Key Metric |
|--------|--------|------------|
| off_transparent | PASS | delta_lufs = 0.0 |
| on_audible | PASS | delta_lufs = 6.0 |
| clip_guard | PASS | clip_count = 0 |
| default_safety | PASS | peak_dbfs = -3.88 |
| low_end_continuity | PASS | max_neighbor_delta_lufs = 0.0 |
| persistence_roundtrip | PASS | roundtrip_stable = 1 |
