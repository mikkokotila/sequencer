# Canonical oracle snapshot path

The frozen benchmark failed before executing: Vite registered the inline HTML module under a /var path and loaded it under /private/var. Return fs.realpath(snapshotRoot) so root, config and HTML proxies share one identity. Frozen source and challenge-bound compiler oracles remain mandatory. Original failure log is retained.

Real-browser verification was not performed for this isolated governance edit; the dependent audio fix run verifies the corrected frozen benchmark. Same PR #41 per explicit user request.
