# S2400 drum export proof

- User authorization: implement the agreed percussion-only native S2400 project export. This explicit feature request overrides the repository's default GA-only product restriction.
- Base: origin/main after PR #44 merged (`88a286f`). No pre-existing uncommitted work.
- Scope: public API and GUI ZIP export, native KIT/S24 records, 48 kHz PCM16 drum WAVs, fixed A1–A5 pads, per-phrase patterns, tempo, levels and mute mask. Unknown format settings retain device-fixture defaults. Exclusions and hardware uncertainty are explicit in GUI/API/docs/archive.
- Targeted verification: nine Chrome tests pass. Independent readers check ZIP CRC, native records, observed hardware trigger bytes, exact sample ends, last-step timing, stereo audio, mutes, levels and 3,840 dense hits. Snapshot edits, live playback, cancellation, sample-budget failures and retry are exercised. The built production GUI also downloads WAV, MP3 and S2400 files successfully.
- Real song verification: LE FEU RESTE exported through the GUI in an isolated Chrome context against the local server. 12 patterns, five samples, 983 drum triggers. Independent Python zipfile/wave/native-record checks pass; no page errors. No SD-card files were changed.
- Visual verification: export dialog screenshot inspected at 1400×1000. Labels, experimental status, limitations, three format controls and Close are visible without overlap.
- Full CI, E2E, contract, architecture and audio gates: authoritative results are generated by the governance compiler in `20260913-s2400-drum-export/proof.manifest.json` and `verdict.json`.
- Limitation: generated S2400 load/playback is NOT VERIFIED. Multi-pattern acceptance, inferred mute/global fields and native level/routing behavior require the device test. Structural round-trip and browser tests do not establish hardware compatibility.
