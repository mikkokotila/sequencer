# Song export encoder dependency

Add pinned @breezystack/lamejs 1.2.7 for browser-worker MP3 encoding. WAV retains the existing PCM encoder. Installation reported zero vulnerabilities. The dependency is LGPL-3.0 and source/license attribution is included with the product documentation.

This dependency configuration slice supports the explicitly requested song-download feature in the same PR. It changes no gate, oracle, baseline, or debt policy. Compiler-executed validation is recorded in verdict.json and gate logs.

Real-browser verification was not performed for this dependency-only slice; the separate song-audio-export proof records GUI and runtime verification.
